# governance-scaffold-r2

> Generated by Codex CLI (model `default`, auth: subscription) on 2026-07-07 06:40:42 CEST.
> Inputs (23): README.md research_log.md docs/INVARIANTS.md docs/learning-resources.md docs/DEVELOPER_GUIDELINES.txt src/xai/__init__.py src/xai/cards.py src/xai/gates.py src/xai/base.py tests/unit/test_xai_cards.py tests/unit/test_xai_gates.py tests/unit/test_stub_contracts.py tests/unit/test_stub_private_branches.py tests/integration/test_xai_smoke.py tools/verify.py tools/check_guidelines.py tools/check_data_cards.py tools/check_notebooks.py .github/workflows/ci.yml notebooks/TEMPLATE.ipynb config/crops/kilifi_crops.yaml docs/data_cards/DC-usda-nass-quickstats.md docs/data_cards/DC-kalro-kilifi-crops.md

---

Verdict: REQUEST-CHANGES. The XAI contract is close, but two correctness gaps still matter: the cards are not actually deeply immutable, and the verification harness can emit `INCOMPLETE` even though the repo docs promise a binary PASS/FAIL outcome. The stub-contract discovery is also heuristic enough that it can miss a graduating callable, which weakens the CI enforcement story.

**Findings**
1. Major, [`src/xai/cards.py`](/home/manyee/Documents/bayesian-crop-yield-forecasting/src/xai/cards.py#L104) and [`src/xai/cards.py`](/home/manyee/Documents/bayesian-crop-yield-forecasting/src/xai/cards.py#L124): the card contract is only shallowly frozen. `ExplanationCard.technical_detail` is wrapped in a `MappingProxyType`, but nested mutable values still leak through, and `RejectionCard.missing_requirements` is stored verbatim, so a caller can pass a list and mutate it after construction. Recommended fix: normalize all collection fields to immutable containers in `__post_init__`, and recursively freeze nested payloads if the “deep-freeze” claim is meant literally.

2. Major, [`tools/verify.py`](/home/manyee/Documents/bayesian-crop-yield-forecasting/tools/verify.py#L10) and [`tools/verify.py`](/home/manyee/Documents/bayesian-crop-yield-forecasting/tools/verify.py#L214): the harness docstring says a hard UNAVAILABLE gate makes the verdict FAIL, but `main()` returns `INCOMPLETE` in that case. That contradicts the README and makes the report format non-binary. Recommended fix: either collapse UNAVAILABLE into FAIL everywhere, or update the docs and downstream consumers to treat `INCOMPLETE` as a first-class verdict.

3. Major, [`tests/unit/test_stub_contracts.py`](/home/manyee/Documents/bayesian-crop-yield-forecasting/tests/unit/test_stub_contracts.py#L114) and [`tests/unit/test_stub_contracts.py`](/home/manyee/Documents/bayesian-crop-yield-forecasting/tests/unit/test_stub_contracts.py#L191): the stub-contract engine is heuristic, not sound. `_collect_cases()` discovers public callables by introspection, and the `len(CASES) >= 40` smoke test only checks volume, not the exact surface, so a missed callable or an imported alias can silently evade the contract. Recommended fix: replace the count smoke test with an explicit expected-name manifest per module, and add regression coverage for descriptor/import/positional-only edge cases.

4. Minor, [`tools/check_data_cards.py`](/home/manyee/Documents/bayesian-crop-yield-forecasting/tools/check_data_cards.py#L9) and [`tools/check_data_cards.py`](/home/manyee/Documents/bayesian-crop-yield-forecasting/tools/check_data_cards.py#L41): the checker calls the frontmatter “YAML” but only supports a narrow pseudo-YAML subset. Nested mappings, block scalars, and inline comments will not behave like real YAML. Recommended fix: either document and lint the restricted subset explicitly, or switch to a real YAML parser plus schema validation.

5. Minor, [`docs/INVARIANTS.md`](/home/manyee/Documents/bayesian-crop-yield-forecasting/docs/INVARIANTS.md#L12): the Enforcement column overstates what is actually automated. LINV-005 and the notebook-existence part of LINV-010 are real gates, but LINV-001/003/006/007/008/009 and the content/execution portion of LINV-010 are mostly review or future-gate policy. Recommended fix: split the column into “Automated gate” and “Review/Planned” so the ledger does not read like hard CI when it is not.

**Checks**
1. `src/xai` correctness: confidence bounds and absent-key handling are correct. `EvidenceGate.evaluate()` implements the documented `>=` semantics, and `evaluate_convergence()` correctly enforces the strict pass rule from the prose, namely `r_hat < 1.01`, `ESS > 400`, `divergences == 0`. The remaining issue is immutability, not threshold logic.

2. Test quality: the XAI unit and smoke tests do pin several real behaviors, especially missing-field rejection, gate semantics, and the end-to-end “gate first, card always” contract. The weak spots are that some tests mirror implementation constants rather than independent behavior, and the stub-contract system is heuristic enough to miss cases. Asserting `NotImplementedError` for pre-graduation stubs is reasonable, but the coverage note is only partly honest because those stub lines inflate coverage until the real implementation lands.

3. Gate tools: current data cards pass `tools/check_data_cards.py`, and `tools/check_notebooks.py` is consistent with the empty graduation ledger. The wiring between `tools/verify.py` and `.github/workflows/ci.yml` is mostly aligned, including the deferred coverage behavior, but the verdict semantics mismatch in `verify.py` still needs fixing.

4. Governance coherence: the invariant set is a mix of hard gates and policy statements. LINV-005 and the notebook-existence clause of LINV-010 are actually enforceable in CI; LINV-001, LINV-002, and LINV-003 are partially enforced through the XAI module and tests; LINV-006, LINV-007, LINV-008, LINV-009, and the content/execution part of LINV-010 are mostly review-only or future work. So yes, some invariants are aspiration dressed as enforcement, unless the ledger is split more explicitly.

5. Curriculum: the milestone plan is coherent with the charter, and the NumPy -> PyMC -> posterior comparison loop is consistent across `research_log.md` and `docs/learning-resources.md`. It is ambitious but structurally realistic for a learning repo. The main mismatch is external to the internal plan, the repo frames Feb to Dec, which is 11 months, not the 9-month brief in the task context.

6. Config/data: `config/crops/kilifi_crops.yaml` is structurally valid YAML, and both data cards satisfy the current checker. Provenance is mostly complete, with `basis`, `kalro_reference`, and the shared metadata fields in place. One thing I would re-check is the `cashew` entry’s Tanzania reference, because it is the odd one out for a Kilifi/KALRO config.

7. Cross-cutting: the strongest contradiction is the verification story, because the docs promise a binary PASS/FAIL report while `verify.py` can emit `INCOMPLETE`. A second softer mismatch is that the README says both dataset tracks are governed by data cards, but the NASS card explicitly defers NASA POWER to a future card, and there is no NASA POWER card in `docs/data_cards/` yet. The graduation path itself is sensible, but it depends on the stub ledger and notebook gate staying tightly synchronized.
