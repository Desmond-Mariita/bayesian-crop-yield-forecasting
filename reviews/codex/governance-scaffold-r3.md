# governance-scaffold-r3

> Generated by Codex CLI (model `default`, auth: subscription) on 2026-07-07 06:51:27 CEST.
> Inputs (23): README.md research_log.md docs/INVARIANTS.md docs/learning-resources.md docs/DEVELOPER_GUIDELINES.txt src/xai/__init__.py src/xai/cards.py src/xai/gates.py src/xai/base.py tests/unit/test_xai_cards.py tests/unit/test_xai_gates.py tests/unit/test_stub_contracts.py tests/unit/test_stub_private_branches.py tests/integration/test_xai_smoke.py tools/verify.py tools/check_guidelines.py tools/check_data_cards.py tools/check_notebooks.py .github/workflows/ci.yml notebooks/TEMPLATE.ipynb config/crops/kilifi_crops.yaml docs/data_cards/DC-usda-nass-quickstats.md docs/data_cards/DC-kalro-kilifi-crops.md

---

Verdict: REQUEST-CHANGES. The new XAI contract itself is mostly sound, and the CI wiring is internally consistent for the current file set. The blockers are governance and graduation mechanics: a couple of invariants claim stronger automation than the tools actually provide, and the stub-discovery path is brittle enough to miss real public APIs when the first curriculum module graduates.

Findings:
1. Major - [docs/INVARIANTS.md](/home/manyee/Documents/bayesian-crop-yield-forecasting/docs/INVARIANTS.md#L23) / [tools/check_data_cards.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tools/check_data_cards.py#L93) / [tools/check_guidelines.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tools/check_guidelines.py#L1)  
   LINV-005 and LINV-007 overstate what is automated. The current card checker validates existing Markdown frontmatter, but it does not prove every consumed dataset has a card or tie code to card coverage; the guideline checker only scans Python source under `src`, so it cannot enforce provenance fields or “no magic numbers” in YAML/config files. Tighten the invariant wording to match the implemented scope, or add dedicated config/provenance enforcement.

2. Major - [tests/unit/test_stub_contracts.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tests/unit/test_stub_contracts.py#L114) / [tools/check_notebooks.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tools/check_notebooks.py#L37)  
   The graduation/discovery engine is structurally brittle. It only sees direct module attributes that are functions or classes with matching `__module__`, and for classes it only sees direct members from `vars(cls)`. That can silently miss inherited methods, callable objects, re-exported wrappers, or other public callable surfaces, which means a real graduation can evade the contract machinery. Replace this with an explicit export manifest or a much more exhaustive surface scan.

3. Minor - [src/xai/cards.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/src/xai/cards.py#L146)  
   `RejectionCard.__post_init__` coerces `missing_requirements` to a tuple but never validates that it is a sequence of non-empty strings. A bare string will become a tuple of characters, which produces a valid-looking but nonsense card. Validate item types and explicitly reject `str`/`bytes`.

1. `src/xai` correctness: mostly clean. `ExplanationCard` and `RejectionCard` enforce closed confidence bounds and non-empty required string fields, and the deep-freeze step prevents mutation leaks from nested payloads. `EvidenceGate` uses `>=` semantics for thresholds and treats absent keys as zero, which matches the intended “evidence before inference” behavior. `evaluate_convergence` is also correct on the documented strict boundaries: `r_hat < 1.01`, `ess > 400`, `divergences == 0` means exact boundary values fail, as intended. The ABC contract in `src/xai/base.py` is a clean interface, but it is interface-only, not runtime enforcement of model behavior. One caveat: the frozen dataclasses are not hashable value objects because their mapping fields are `MappingProxyType`, so if hashability matters later, that needs a redesign.

2. Test quality: the XAI unit and smoke tests do pin real behavior, especially the confidence bounds, gate boundaries, absent-key semantics, and end-to-end rejection/explanation flow. The weaker parts are implementation-coupled assertions: tests check `MappingProxyType` behavior, exact tuple coercion, and string fragments, which is acceptable for a contract layer but narrower than pure black-box testing. The stub-contract suite is the fragile piece: it is a source-shape oracle, not a semantic oracle. Using `NotImplementedError` raises to preserve pre-implementation coverage is defensible as scaffolding, but it is not honest evidence of meaningful behavioral coverage by itself; it only becomes meaningful once graduated modules replace the stubs with real tests.

3. Gate tools: `check_data_cards.py` is fine for the attached cards. Its restricted parser handles the current frontmatter format, colons in scalar values, and whitespace-prefixed comment stripping safely; nested/indented YAML and block scalars are deliberately unsupported rather than accidentally misparsed. `check_notebooks.py` is the fragile one because it couples notebook enforcement to a literal ledger in `tests/unit/test_stub_contracts.py` via AST. `tools/verify.py` and `.github/workflows/ci.yml` are consistent overall: same hard gates, advisory `mypy`, and the same coverage-defer logic until tests exist.

4. Governance coherence: the invariant set is mixed. LINV-003, LINV-005 as schema validation, and LINV-010 existence are genuinely enforceable. LINV-004, LINV-006, LINV-008, and LINV-009 are partly review-only or only weakly enforced. LINV-007 is the clearest mismatch: the stated enforcement chain does not actually verify provenance-bearing config constants or magic-number avoidance outside Python source. That one reads as aspiration dressed as enforcement.

5. Curriculum: the phase structure is coherent on its own terms. The phase 1 and phase 2 week counts are plausible, the NumPy -> PyMC -> posterior-comparison loop is clear, and the reading list tracks the Bayesian milestones well. The only weakness is operational detail: phases 3 and 4 are much less specified than phases 1 and 2, so the later curriculum is more aspirational than executable. I did not see a direct contradiction between the milestone plan and the resource list.

6. Config/data: the Kilifi YAML is structurally valid and the provenance is complete enough for the current schema. Every crop entry shown carries both `basis` and `kalro_reference`, and the data cards match the checker’s required fields and the surrounding prose. No schema mismatch jumped out between the cards and the config. The NASS card is also internally consistent: it honestly marks the downloader as still stubbed and describes the planned processing/validation path.

7. Cross-cutting: the main cross-document issue is not README versus CI, because those are aligned, but docs versus enforcement scope. The repo says “CI-enforced invariants” and “before any code consumes it” in places where the tools only validate existing files, not dataset/code relationships. The graduation path will work only if implementers update `IMPLEMENTED` in the same commit and stay within the discovery heuristic’s current shape assumptions. That is the main breakage risk when the first real curriculum stub graduates.
