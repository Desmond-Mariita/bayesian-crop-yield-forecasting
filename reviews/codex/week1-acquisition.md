# week1-acquisition

> Generated by Codex CLI (model `default`, auth: subscription) on 2026-07-08 00:40:04 CEST.
> Inputs (17): src/data/acquisition.py tests/unit/test_acquisition.py tests/unit/test_weather_acquisition.py tests/integration/test_acquisition_smoke.py tests/unit/test_stub_contracts.py src/curriculum_ledger.py tools/check_notebooks.py tools/verify.py pyproject.toml notebooks/data/acquisition.ipynb notebooks/TEMPLATE.ipynb data/samples/nass_quickstats_sample.json data/samples/nasa_power_sample.json docs/data_cards/DC-usda-nass-quickstats.md docs/data_cards/DC-nasapower-weather.md docs/INVARIANTS.md README.md

---

Verdict: APPROVE-WITH-CHANGES. The acquisition code is behaving correctly on the semantics you called out, and the graduation machinery is wired in a way that should keep the dummy contract calls off the network. I found one real test-quality gap in the POWER failure path and one stale docstring claim about `data_source` tagging; neither is a release blocker, but both should be fixed to keep the repo’s governance story consistent.

**Findings**
1. Major — [`tests/unit/test_weather_acquisition.py:135`](file:///home/manyee/Documents/bayesian-crop-yield-forecasting/tests/unit/test_weather_acquisition.py#L135) (`test_fetch_failure_propagates`): this test short-circuits the transport layer by monkeypatching `_fetch_json`, so it does not exercise the real `urllib.request.urlopen` error mapping for `download_weather_data`. Why it matters: a regression in the POWER entry point’s transport wiring or error translation could slip through even though the NASS fake-HTTP smoke tests pass. Recommended fix: add POWER-specific fake-HTTP coverage that monkeypatches `urllib.request.urlopen` to raise `URLError` and to return invalid JSON, and assert `download_weather_data` raises `ConnectionError` in both cases.
2. Minor — [`src/data/acquisition.py:10`](file:///home/manyee/Documents/bayesian-crop-yield-forecasting/src/data/acquisition.py#L10) to `:13` (module docstring): the top-level docstring says every record written by the module carries `data_source` (`api_nass`) and points only at the NASS card. Why it matters: the module now also writes POWER rows tagged `api_nasa_power`, so the statement is stale and can mislead readers/reviewers. Recommended fix: split the note by function or rewrite it to mention both tags explicitly.

**Checks**
1. Acquisition correctness: clean on behavior. The NASS query pins `commodity_desc`, `year__GE`, `agg_level_desc=COUNTY`, `statisticcat_desc=YIELD`, `source_desc=SURVEY`, and JSON output in [`src/data/acquisition.py:67`](file:///home/manyee/Documents/bayesian-crop-yield-forecasting/src/data/acquisition.py#L67); the POWER URL pins `parameters`, `community=AG`, coordinates, and the date window in [`src/data/acquisition.py:189`](file:///home/manyee/Documents/bayesian-crop-yield-forecasting/src/data/acquisition.py#L189). The comma stripping, `(D)`→NaN coercion, `-999`→NaN fill handling, `data_source` tagging, and env-only NASS key handling are all implemented and pinned by tests/docs.
2. Test quality: mostly good and fully offline. The NASS unit and smoke tests cover the happy path plus both fake-HTTP failure modes (`URLError` and invalid JSON), and the POWER unit tests cover payload shaping, `-999` handling, and validation-before-network. The only gap is that POWER’s real transport failure path is not exercised through the fake HTTP layer; the current failure test patches `_fetch_json` instead of `urlopen`.
3. Graduation mechanics: sound. With both names in `IMPLEMENTED`, the contract engine’s dummy call for NASS dies immediately on missing `NASS_API_KEY`, and the POWER dummy call dies on `year_start=2` before any network use. That ordering is load-bearing and it is documented/tested in the function docs and the unit tests.
4. Notebook + execution gate: sound. [`tools/verify.py:113`](file:///home/manyee/Documents/bayesian-crop-yield-forecasting/tools/verify.py#L113) makes notebook execution a hard gate when notebooks exist, runs with `--no-cov`, and defers cleanly when there are no notebooks. The bootstrap cell in both notebooks resolves the repo root from `pyproject.toml`, and the companion notebook uses `inspect.getsource` plus bundled sample JSON, so it respects both the source-display rule and the offline rule.
5. Data cards: accurate. Both cards match the code on parameters, units, limitations, tags, and the “no rainfall here / CHIRPS later” split. I did not see a card claim that the implementation fails to honor.
6. Cross-cutting: the invariant/README claims match the actual gates. [`docs/INVARIANTS.md:28`](file:///home/manyee/Documents/bayesian-crop-yield-forecasting/docs/INVARIANTS.md#L28) and [`README.md:99`](file:///home/manyee/Documents/bayesian-crop-yield-forecasting/README.md#L99) align with the real `check_notebooks` + `nbmake` wiring, and the ledger-driven graduation flow should scale to a third stub. The main operational caveat is that `verify.py` will execute every `*.ipynb` under `notebooks/`, so any future scratch notebook will need to be offline too or excluded by policy.
