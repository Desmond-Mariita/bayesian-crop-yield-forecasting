# governance-scaffold

> Generated by Codex CLI (model `default`, auth: subscription) on 2026-07-07 06:21:57 CEST.
> Inputs (22): README.md research_log.md docs/INVARIANTS.md docs/learning-resources.md docs/DEVELOPER_GUIDELINES.txt src/xai/__init__.py src/xai/cards.py src/xai/gates.py src/xai/base.py tests/unit/test_xai_cards.py tests/unit/test_xai_gates.py tests/unit/test_stub_contracts.py tests/unit/test_stub_private_branches.py tests/integration/test_xai_smoke.py tools/verify.py tools/check_data_cards.py tools/check_notebooks.py .github/workflows/ci.yml notebooks/TEMPLATE.ipynb config/crops/kilifi_crops.yaml docs/data_cards/DC-usda-nass-quickstats.md docs/data_cards/DC-kalro-kilifi-crops.md

---

Verdict: REQUEST-CHANGES. The XAI contract is close, and the evidence-gate smoke path is good, but there is a real invariant mismatch at the convergence boundary, the card immutability story is only shallow, and the graduation/notebook governance can be bypassed or overstates what it actually enforces. The curriculum docs also drift on calendar length, so the repo’s claims are ahead of the automation.

1. Blocker - [src/xai/gates.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/src/xai/gates.py#L132) and [docs/INVARIANTS.md](/home/manyee/Documents/bayesian-crop-yield-forecasting/docs/INVARIANTS.md#L14). The convergence gate is inclusive at the threshold (`>` / `<` checks), so `r_hat == 1.01` and `ess_* == 400` pass even though the invariant text says `R-hat < 1.01` and `ESS > 400`. Fix by either making equality fail or updating the invariant and tests to explicitly allow inclusive thresholds.

2. Major - [src/xai/cards.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/src/xai/cards.py#L90) and [src/xai/gates.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/src/xai/gates.py#L39). `frozen=True` is only shallow here: `technical_detail`, `GateStatus.current`, and `GateStatus.required` are mutable mappings, so callers can mutate the contents after construction, and `ExplanationCard` is not safely hashable with the default `dict` payload. If the intent is true immutability, convert the mappings to immutable structures in `__post_init__`; otherwise document that the payloads are intentionally mutable/unhashable.

3. Major - [tests/unit/test_stub_contracts.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tests/unit/test_stub_contracts.py#L109) and [tests/unit/test_stub_contracts.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tests/unit/test_stub_contracts.py#L180). The reflection sweep only sees directly defined public functions/classes, so it can miss classmethods, staticmethods, properties, re-exports, and dynamically attached callables. The `IMPLEMENTED` skip also creates a bypass: a stub can be marked graduated early and the contract test will silently skip it. Replace this with an explicit manifest or source-derived API list, and fail on ledger entries that are not backed by real behavior tests.

4. Major - [docs/INVARIANTS.md](/home/manyee/Documents/bayesian-crop-yield-forecasting/docs/INVARIANTS.md#L21), [README.md](/home/manyee/Documents/bayesian-crop-yield-forecasting/README.md#L99), [tools/check_notebooks.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tools/check_notebooks.py#L121), and [.github/workflows/ci.yml](/home/manyee/Documents/bayesian-crop-yield-forecasting/.github/workflows/ci.yml#L30). The notebook rule is described as if it enforces content and execution discipline, but the gate only checks file existence, and CI does not run nbmake or verify that the notebook displays source via `inspect.getsource`. That is governance language outpacing enforcement. Either narrow the wording to “existence only” or add real content/execution checks.

5. Minor - [tools/check_data_cards.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tools/check_data_cards.py#L9) and [tools/check_data_cards.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tools/check_data_cards.py#L73). The parser is a constrained line-based subset, not YAML. Inline comments become part of the value, and richer nested structures are not supported. That is acceptable for the current cards, but the docstring should say “restricted frontmatter syntax” or the checker should move to a real YAML parser plus schema validation.

6. Minor - [README.md](/home/manyee/Documents/bayesian-crop-yield-forecasting/README.md#L16) and [research_log.md](/home/manyee/Documents/bayesian-crop-yield-forecasting/research_log.md#L21). The charter says this is a 9-month repo, but the phase plan spans Feb through Dec, which is 11 months. If that is intentional, say so explicitly; otherwise compress the schedule or update the charter language.

1. XAI correctness. Clean on basic validation semantics: `ExplanationCard` rejects empty required strings and out-of-range confidence, `EvidenceGate.evaluate()` treats absent keys as zero, and the evidence gate uses `>=` semantics at the threshold. The only correctness issue is the convergence boundary mismatch noted above. `ExplainableModel` as an abstract base class is fine as an ABC contract.

2. Test quality. The XAI unit and smoke tests do pin some behavior, especially absent-key handling, below-gate rejection, and the end-to-end card return path. They still mirror the implementation in places: they do not catch the shallow mutability/hashability leak, and they do not test convergence boundaries. The stub-contract tests are useful as scaffold sentinels, but they are not sound as a full discovery mechanism. Asserting `NotImplementedError` is honest only as a temporary scaffold contract; it is not honest evidence of implemented behavior once a module graduates.

3. Gate tools. `check_data_cards.py` is fine for the current cards, but it is intentionally not a general YAML parser. `check_notebooks.py` is structurally fragile because it keys off a test-file constant and only checks notebook presence. `verify.py` and CI are mostly consistent on which gates they run, but CI reimplements the steps instead of invoking `tools/verify.py`, so the timestamped report artifact remains a manual-step artifact rather than a CI artifact.

4. Governance coherence. Not all 10 invariants are actually enforceable as written. LINV-001 to LINV-003 are the most enforceable because they are backed by code contracts and tests. LINV-005 is partly enforceable via the data-card checker. LINV-010 is only partially enforced because existence is checked, not notebook content or execution. LINV-004, LINV-006, LINV-007, LINV-008, and LINV-009 are mostly process/review commitments, not mechanical CI gates, so the enforcement column overstates automation.

5. Curriculum. The structure is coherent with the charter: NumPy first, then PyMC/ArviZ comparison, then broader XAI and Kilifi work. The resource list also matches the phase split reasonably well. The realism issue is the calendar: the phase plan is effectively an 11-month plan, not a 9-month one. That does not break the pedagogy, but it should be reconciled.

6. Config/data. `config/crops/kilifi_crops.yaml` is structurally valid YAML and the provenance metadata is better than minimal. Both data cards satisfy the checker schema and are consistent with the README and research log. The cards are still marked `draft`, which is allowed by the current validator.

7. Cross-cutting. The main contradiction is between strong governance language and partial enforcement, especially for notebooks and most invariants outside XAI. The graduation path will also be brittle the first time a stub becomes real unless the team remembers to update `IMPLEMENTED` and replace the scaffold-style contract test with behavior tests. No other immediate breakage stands out as severe as the convergence boundary mismatch.
