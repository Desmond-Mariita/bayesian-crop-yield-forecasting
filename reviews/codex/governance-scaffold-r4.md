# governance-scaffold-r4

> Generated by Codex CLI (model `default`, auth: subscription) on 2026-07-07 19:23:22 CEST.
> Inputs (23): README.md research_log.md docs/INVARIANTS.md docs/learning-resources.md docs/DEVELOPER_GUIDELINES.txt src/xai/__init__.py src/xai/cards.py src/xai/gates.py src/xai/base.py tests/unit/test_xai_cards.py tests/unit/test_xai_gates.py tests/unit/test_stub_contracts.py tests/unit/test_stub_private_branches.py tests/integration/test_xai_smoke.py tools/verify.py tools/check_guidelines.py tools/check_data_cards.py tools/check_notebooks.py .github/workflows/ci.yml notebooks/TEMPLATE.ipynb config/crops/kilifi_crops.yaml docs/data_cards/DC-usda-nass-quickstats.md docs/data_cards/DC-kalro-kilifi-crops.md

---

**Verdict:** REQUEST-CHANGES. The XAI core is directionally sound and the new gates pass on the attached cards, but the graduation path is brittle: one branch-level stub test blocks incremental implementation, and the notebook-gate discovery is coupled too tightly to a test-file literal. I also found one correctness gap in the new XAI cards: enum-typed fields are not runtime-validated, so the structured-card contract can still be violated with plain strings.

**Findings**
1. **Major** - [tests/unit/test_stub_private_branches.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tests/unit/test_stub_private_branches.py#L31-L36) assumes both `LinearRegression` fit paths are still stubs at the same time, which conflicts with the milestone split in [research_log.md](/home/manyee/Documents/bayesian-crop-yield-forecasting/research_log.md#L48-L49). The first real implementation of either `closed_form` or `gradient_descent` will fail this test even if the other branch is intentionally still a stub. Recommended change: split the branch expectations so each branch can graduate independently, or retire this branch-level stub assertion once the public contract graduates.

2. **Major** - [tools/check_notebooks.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tools/check_notebooks.py#L37-L72) is coupled to a literal `IMPLEMENTED` assignment in [tests/unit/test_stub_contracts.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tests/unit/test_stub_contracts.py#L38-L42). That makes CI brittle under harmless refactors and can also read the wrong same-named assignment if the file ever grows one. Recommended change: move the graduation ledger to a dedicated machine-readable file, or at least restrict the parser to the top-level `IMPLEMENTED` assignment only.

3. **Major** - [tests/unit/test_stub_contracts.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tests/unit/test_stub_contracts.py#L120-L191) and [tests/unit/test_stub_contracts.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/tests/unit/test_stub_contracts.py#L196-L274) discover the public surface by direct module/class dict inspection. That is shape-dependent rather than behavior-dependent, so callable objects, inherited public methods, and some descriptor-based APIs are invisible to the contract engine. Recommended change: either document this exact source-shape constraint as part of the curriculum contract, or replace discovery with a more explicit manifest/AST-based mechanism.

4. **Minor** - [src/xai/cards.py](/home/manyee/Documents/bayesian-crop-yield-forecasting/src/xai/cards.py#L85-L162) validates confidence and non-empty strings, but it does not runtime-check the enum-typed fields (`data_quality` and `rejection_code`). As written, a caller can still construct a card with plain strings, which weakens the “structured card” contract the docs emphasize. Recommended change: add `isinstance(..., DataQuality/RejectionCode)` validation in `__post_init__` for both cards.

**Checks**
1. **src/xai correctness:** Mostly clean. `ExplanationCard` and `RejectionCard` correctly enforce confidence bounds and empty-field checks, `EvidenceGate` uses `>=` semantics with absent keys treated as zero, and `evaluate_convergence` is strict at the documented boundaries (`r_hat < 1.01`, `ESS > 400`, `divergences == 0`). The ABC contract is minimal but valid: it forces the `explain_or_reject` method to exist, though it does not enforce semantics beyond that. One residual gap is the enum validation noted above. I also confirmed the frozen cards are immutable, but they are not hashable because `MappingProxyType` is unhashable.

2. **Test quality:** The XAI tests do pin current behavior reasonably well, but some of them mirror the implementation closely rather than independently specifying behavior. The convergence-boundary test is effectively a lock on the exact boundary semantics in `src/xai/gates.py`, which is fine, but the integration smoke test only proves the contract for a toy in-test model, not for future real model classes. The stub-contract engine is not fully sound: it is intentionally source-shape-driven and can miss legitimate refactors or nonstandard callable forms. Using `NotImplementedError` as a pre-implementation coverage scaffold is honest as a placeholder, but it is not a behavior-quality signal. I could not run `pytest` in this sandbox because the environment has no usable writable temp directory; pytest failed before collection with `FileNotFoundError` from `tempfile`.

3. **Gate tools:** `tools/check_data_cards.py` passes the attached cards and is fine for the current flat frontmatter, including colons in URLs and simple list fields. Its parser is intentionally not real YAML, though: nested mappings, block scalars, anchors, and some quoted `#` edge cases are unsupported by design. `tools/check_notebooks.py` is the fragile piece because it binds CI to a test-file literal. `tools/verify.py` and `.github/workflows/ci.yml` are consistent: CI runs the same verifier and uploads the verification artifact.

4. **Governance coherence:** The docs are more honest than they first appear. `docs/INVARIANTS.md` explicitly labels some items as review-only or planned, so not every invariant is claimed to be CI-enforced. That said, LINV-001/002/003/008/009 are only partially enforceable in practice; they are mostly unit-test- or process-enforced, not repository-wide automated gates. LINV-004/006/007/010 are candidly review-only or existence-only. So there is no outright contradiction, but there is plenty of “policy with tests” rather than hard enforcement.

5. **Curriculum:** The phase structure is coherent with the charter: Feb-Dec is split into three 3-month core phases plus a 2-month XAI/capstone phase, and the learning resources line up with those phases. The week counts are plausible, and the evidence-gate loop in Phase 2 matches the NumPy → PyMC → compare charter. The only real tension is the graduation path, where some tests force all-or-nothing completion for a public method rather than allowing incremental branch graduation.

6. **Config/data:** Both data cards pass the schema gate, and `config/crops/kilifi_crops.yaml` is structurally valid in the attached file. The Kilifi config carries `basis` and `kalro_reference` fields for each crop, which is the right provenance shape. The weak point is traceability: several entries use broad references like `literature` or fairly generic provenance strings, so the narrative is stronger than the source specificity. I did not find a schema violation in either card.

7. **Cross-cutting:** The biggest cross-cutting issue is the graduation path. `research_log.md` assumes incremental weekly implementation, but `tests/unit/test_stub_private_branches.py` makes `LinearRegression` effectively all-or-nothing by asserting both branches are still stubs. That will be the first thing to break when a real stub starts graduating. A smaller coherence issue is that [docs/DEVELOPER_GUIDELINES.txt](/home/manyee/Documents/bayesian-crop-yield-forecasting/docs/DEVELOPER_GUIDELINES.txt#L22-L26) still refers to `configs/` and `src/utils/config.py`, which does not match the attached tree’s `config/` layout.

**Verification note:** I ran `tools/check_data_cards.py` and `tools/check_notebooks.py` on the attached files; both passed.
