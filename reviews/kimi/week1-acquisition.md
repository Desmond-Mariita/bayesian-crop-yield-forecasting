# week1-acquisition

> Generated by Kimi (`kimi-k2.6`) via Moonshot Anthropic-compatible endpoint + Claude Code harness on 2026-07-08 01:36:08 CEST.
> Inputs (18): data/samples/nasa_power_sample.json data/samples/nass_quickstats_sample.json docs/data_cards/DC-nasapower-weather.md docs/data_cards/DC-usda-nass-quickstats.md docs/INVARIANTS.md notebooks/data/acquisition.ipynb notebooks/TEMPLATE.ipynb README.md src/curriculum_ledger.py src/data/acquisition.py src/utils/config.py tests/integration/test_acquisition_smoke.py tests/unit/test_acquisition.py tests/unit/test_config.py tests/unit/test_stub_contracts.py tests/unit/test_weather_acquisition.py tools/check_notebooks.py tools/verify.py

---

The review is complete and saved to disk at `reviews/kimi/week1-acquisition.md`.

**Verdict: APPROVE-WITH-CHANGES**

The Week 1 increment is solid overall — both acquisition functions are correct, genuinely offline-testable, and the graduation mechanics are load-bearing. I flagged **6 findings**:

1. **Major** — `notebooks/TEMPLATE.ipynb` is executed by the `nbmake` hard gate, but it contains placeholder imports (`src.xai.gates.evaluate_convergence`) that aren't tied to any graduated module. If that API ever shifts, CI breaks for an unrelated reason. Move the template out of `notebooks/`.
2. **Minor** — `_power_to_frame` assumes JSON insertion order matches `POWER_PARAMETERS`, but doesn't enforce it. The test is strict about column order; better to select columns explicitly in code.
3. **Minor** — Copy-paste residue in `test_weather_acquisition.py:141` (error message says "NASS Quick Stats" in the POWER test).
4. **Minor** — `_records_to_frame` warns on missing expected columns but doesn't guard against a missing `Value` column, which would raise `KeyError` instead of the documented `ConnectionError`.
5. **Nit** — `download_nass_yields` lacks year sanity validation (unlike `download_weather_data`).
6. **Nit** — `get_env` treats empty strings as unset, which is undocumented.

All six mandated checks are addressed explicitly in the document.
