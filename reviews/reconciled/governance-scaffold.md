# governance-scaffold

> Generated by aichat + DeepSeek (`deepseek:deepseek-v4-flash`, role `auditor`) on 2026-07-07 06:25:53 CEST.
> Inputs (4): reviews/deepseek/governance-scaffold.md reviews/gemini/governance-scaffold.md reviews/codex/governance-scaffold.md reviews/glm/governance-scaffold.md

---

# Reconciled Review Report: governance-scaffold

## Summary
This report consolidates findings from four independent reviews of the same package. Each finding is listed once with all reviewer citations, severities, and exact file/clause references. Findings are ordered by the highest severity any reviewer assigned. Disagreements are explicitly flagged.

---

## Findings

### 1. (Blocker) Missing `tools/check_guidelines.py`
- **Raised by:** DeepSeek (Blocker)
- **Not raised by:** Gemini, Codex, GLM
- **Exact citations:** `README.md` (line “tools/check_guidelines.py”), `docs/INVARIANTS.md` (LINV-007 Enforcement column), `tools/verify.py` (line calling it), `.github/workflows/ci.yml` (step “Developer guidelines gate”)
- **Description:** The file `tools/check_guidelines.py` is referenced in the project structure, CI pipeline, verification harness, and invariant enforcement but is not present in the attached set. CI will fail on every push, `verify.py` will report an `INCOMPLETE` verdict, and invariant LINV-007 is unenforceable.
- **Divergence:** Only DeepSeek reports this as a finding. The other reviewers did not mention it, implying they either did not consider it a blocker or overlooked it.

### 2. (Blocker) Convergence threshold documentation/code inconsistency
- **Raised by:** DeepSeek (Minor), Gemini (Major), Codex (Blocker), GLM (Major)
- **Exact citations:**
  - `src/xai/gates.py` lines 69–70 (DeepSeek), lines 107–114 (Gemini), line 132 (Codex)
  - `docs/INVARIANTS.md` LINV-003 (all)
  - `research_log.md` milestone plan (DeepSeek)
- **Description:** Documentation states “R‑hat < 1.01” and “ESS > 400” (strict inequalities), but the implementation uses non‑strict comparisons (`r_hat > R_HAT_MAX`, `ess_bulk < ESS_BULK_MIN`), so boundary values (exactly 1.01 or 400) pass when the documentation says they should fail. This violates the project’s contract rigor.
- **Divergence on severity:** Codex rates this as **Blocker**; DeepSeek as **Minor**; Gemini and GLM as **Major**. There is no disagreement on the existence of the issue, only on its criticality.

### 3. (Major) Mutable default in frozen dataclass / shallow immutability
- **Raised by:** DeepSeek (Minor), Gemini (Major), Codex (Major)
- **Exact citations:**
  - `src/xai/cards.py` line 56 (DeepSeek), line 91 (Gemini), line 90 (Codex)
  - `src/xai/gates.py` line 39 (Codex)
- **Description:** `ExplanationCard` is declared `frozen=True`, but `technical_detail` has a mutable `dict` default. After construction, the dictionary’s contents can be mutated, breaking the immutability contract. Additionally, Codex reports that `GateStatus.current` and `GateStatus.required` are mutable mappings, making the frozen dataclass only shallowly immutable.
- **Divergence:** DeepSeek rates this **Minor**; Gemini and Codex rate it **Major**. No disagreement on the existence.

### 4. (Major) Stub‑discovery engine may miss callables / IMPLEMENTED bypass
- **Raised by:** DeepSeek (Minor), Codex (Major)
- **Exact citations:**
  - `tests/unit/test_stub_contracts.py` line 123 (DeepSeek), lines 109 and 180 (Codex)
- **Description:** The dynamic discovery filter (`getattr(obj, "__module__", None) != module_name`) skips callables that are re‑exported from private sub‑modules, classmethods, staticmethods, properties, or dynamically attached callables. Codex additionally notes that the `IMPLEMENTED` skip creates a bypass: a stub can be marked graduated early and the contract test will silently skip it.
- **Divergence:** DeepSeek rates **Minor**; Codex rates **Major**.

### 5. (Major) Notebook rule only checks existence, not content or execution
- **Raised by:** Codex (Major)
- **Not raised by:** DeepSeek, Gemini, GLM
- **Exact citations:** `docs/INVARIANTS.md` line 21, `README.md` line 99, `tools/check_notebooks.py` line 121, `.github/workflows/ci.yml` line 30
- **Description:** The governance language describes the notebook rule as if it enforces content and execution discipline, but the gate only checks file existence. CI does not run `nbmake` or verify that the notebook displays source via `inspect.getsource`. Wording should be narrowed or real checks added.

### 6. (Major) Data‑card validator creates empty lists from empty strings
- **Raised by:** GLM (Major)
- **Not raised by:** DeepSeek, Gemini, Codex
- **Exact citations:** (GLM’s review does not give a line number; inferred to be in `tools/check_data_cards.py`) 
- **Description:** A card with `- ""` list items passes validation (non‑empty list) but is semantically empty. This creates a false sense of data presence.

### 7. (Major) Convergence gate type mismatch (`divergences: int` stored as float)
- **Raised by:** GLM (Major)
- **Not raised by:** DeepSeek, Gemini, Codex
- **Exact citations:** `src/xai/gates.py` `evaluate_convergence` signature and implementation
- **Description:** The function signature declares `divergences: int` but the implementation stores it as a float, causing confusing internal logic. This is a distinct issue from the boundary mismatch (Finding #2).

### 8. (Minor) CI duplicates logic from `verify.py`
- **Raised by:** Gemini (Minor)
- **Not raised by:** Others
- **Exact citations:** `.github/workflows/ci.yml` lines 52–56
- **Description:** The CI workflow re‑implements the individual check steps (guidelines, data cards, notebooks, flake8, black, mypy, pytest) instead of calling `tools/verify.py` directly, creating a maintenance burden and risk of divergence.

### 9. (Minor) Aspirational invariants without automated enforcement (LINV‑004, LINV‑006)
- **Raised by:** Gemini (Minor)
- **Not raised by:** Others (Codex’s Finding #5 partially overlaps but is about notebook existence, not these specific invariants)
- **Exact citations:** `docs/INVARIANTS.md` LINV‑004, LINV‑006
- **Description:** LINV‑004 (“Missing data is a model input”) and LINV‑006 (“Wide priors”) list “review” or “preprocessing tests” as enforcement, unlike other invariants with dedicated tools. The “governance by CI” claim is overstated.

### 10. (Minor) Data‑card parser is line‑based, not YAML
- **Raised by:** Codex (Minor)
- **Not raised by:** Others
- **Exact citations:** `tools/check_data_cards.py` lines 9, 73
- **Description:** The frontmatter parser is a constrained line‑based subset, not a real YAML parser. Inline comments become part of the value, and nested structures are unsupported. Acceptable for current cards but docstring should say “restricted frontmatter syntax”.

### 11. (Minor) Schedule length mismatch (9‑month repo vs 11‑month plan)
- **Raised by:** Codex (Minor)
- **Not raised by:** Others
- **Exact citations:** `README.md` line 16, `research_log.md` line 21
- **Description:** The charter says this is a 9‑month repository, but the phase plan spans February through December (11 months). Should be reconciled.

### 12. (Minor) Coverage warning for NotImplementedError pattern not documented
- **Raised by:** GLM (Minor)
- **Not raised by:** Others
- **Description:** The stub‑contract engine uses `NotImplementedError` to achieve 90%+ coverage before real implementation exists. This is an honest use of the gate, but the coverage gate passes without automated enforcement during graduation. Should be documented.

### 13. (Minor) `DataQuality` enum value not constrained in return type hint
- **Raised by:** GLM (Minor)
- **Not raised by:** Others
- **Exact citations:** (GLM’s review mentions nit; no exact line given)
- **Description:** The return type of `ExplainableModel.explain_or_reject` does not constrain the `DataQuality` enum value, which is a minor typing improvement.

### 14. (Minor) “Infrastructure modules” in LINV‑010 undefined
- **Raised by:** GLM (Minor)
- **Not raised by:** Others
- **Exact citations:** `docs/INVARIANTS.md` LINV‑010
- **Description:** LINV‑010 exempts “infrastructure modules” from the notebook requirement, but the term is not defined, creating ambiguity.

### 15. (Minor) Test gap for empty‑list field validation and `technical_detail` mapping
- **Raised by:** GLM (Minor)
- **Not raised by:** Others
- **Exact citations:** (GLM’s review mentions “test gap for empty-list field validation and technical_detail Mapping field in test_xai_cards.py”)
- **Description:** Current tests do not cover the case of empty‑list fields in data cards or the mutability of `technical_detail` (see Finding #3 for the underlying issue).

---

## Per‑Reviewer Verdict Table

| Reviewer | Verdict / Severity Summary |
|----------|----------------------------|
| DeepSeek | REQUEST‑CHANGES (1 Blocker, 3 Minor) |
| Gemini   | APPROVE‑WITH‑CHANGES (2 Major, 2 Minor) |
| Codex    | REQUEST‑CHANGES (1 Blocker, 3 Major, 2 Minor) |
| GLM      | APPROVE‑WITH‑CHANGES (2 Major, 5 Minor) |

## Consensus Verdict

**Not‑acceptable‑as‑drafted** – Two of four reviewers (DeepSeek, Codex) assign Blocker‑level findings. All reviewers identify at least one Major issue. The package cannot be accepted without resolving all Blocker and Major findings.

## Action Before Sign‑Off

The following findings (every Blocker or Major raised by at least one reviewer) must be addressed before sign‑off:

1. **Missing `tools/check_guidelines.py`** – Create the file or remove all references.
2. **Convergence threshold boundary mismatch** – Align documentation with implementation or vice versa; add boundary tests.
3. **Mutable default in frozen dataclass / shallow immutability** – Convert mutable mappings to immutable proxies or document the intentional leak.
4. **Stub‑discovery engine limitations** – Extend discovery to catch re‑exports, classmethods, etc., and remove IMPLEMENTED bypass risk.
5. **Notebook rule overstates enforcement** – Either add content/execution checks or narrow governance language.
6. **Data‑card validator creates empty lists from empty strings** – Reject or warn on `- ""` items.
7. **Convergence gate type mismatch** – Fix `divergences` type and internal logic in `evaluate_convergence`.

## One‑Line Reminder

A single reviewer’s clearance is not sign‑off; all Blocker and Major findings must be resolved to the satisfaction of the project’s designated approver.
