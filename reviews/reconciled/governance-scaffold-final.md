# governance-scaffold-final

> Generated by aichat + DeepSeek (`deepseek:deepseek-v4-flash`, role `auditor`) on 2026-07-07 06:54:54 CEST.
> Inputs (4): reviews/deepseek/governance-scaffold-r2.md reviews/glm/governance-scaffold-r2.md reviews/gemini/governance-scaffold-r3.md reviews/codex/governance-scaffold-r3.md

---

# Reconciled Review Report: governance-scaffold

**Files reviewed (same package, 23 inputs each):**  
- `reviews/deepseek/governance-scaffold-r2.md` (DeepSeek)  
- `reviews/glm/governance-scaffold-r2.md` (GLM)  
- `reviews/gemini/governance-scaffold-r3.md` (Gemini)  
- `reviews/codex/governance-scaffold-r3.md` (Codex)

---

## Findings (ordered by highest severity assigned by any reviewer)

### 🔴 Major

#### 1. Invariant automation scope overstated (LINV-005, LINV-007)
| Reviewer(s) | Severity | Exact reference |
|-------------|----------|----------------|
| **Codex**   | Major    | `reviews/codex/governance-scaffold-r3.md` – Finding 1: “LINV-005 and LINV-007 overstate what is automated” – cites `docs/INVARIANTS.md#L23`, `tools/check_data_cards.py#L93`, `tools/check_guidelines.py#L1` |
| GLM         | Minor (aligned issue on LINV-004 but not on these) | – |
| DeepSeek    | Not raised | – |
| Gemini      | Not raised | – |

**Description:**  
Codex states that LINV-005 and LINV-007 in `docs/INVARIANTS.md` claim stronger automation than the tools actually implement. The card checker (`check_data_cards.py`) validates existing frontmatter but does not prove every consumed dataset has a card or tie code coverage; the guideline checker (`check_guidelines.py`) only scans Python under `src/`, so it cannot enforce provenance fields or “no magic numbers” in YAML/config files. Codex recommends either tightening the invariant wording or adding dedicated config/provenance enforcement.

**Consensus status: no agreement on severity** – Codex treats this as a **Major** blocker; the other three reviewers either do not flag it (DeepSeek, Gemini) or flag a similar issue only for LINV-004 (GLM, Minor). No other reviewer disputes the overstatement claim explicitly, but they did not highlight the same invariants.

---

#### 2. Graduation / stub-discovery engine structurally brittle
| Reviewer(s) | Severity | Exact reference |
|-------------|----------|----------------|
| **Codex**   | Major    | `reviews/codex/governance-scaffold-r3.md` – Finding 2: “The graduation/discovery engine is structurally brittle” – cites `tests/unit/test_stub_contracts.py#L114`, `tools/check_notebooks.py#L37` |
| Gemini      | Not raised (calls the design “creative and effective”, “robust”) | – |
| DeepSeek    | Not raised (calls it “sound”) | – |
| GLM         | Not raised (calls it “sound” and “honest and correct”) | – |

**Description:**  
Codex argues that the dynamic discovery in `test_stub_contracts.py` only sees direct module attributes that are functions or classes with matching `__module__`, and for classes only `vars(cls)` direct members. This can silently miss inherited methods, callable objects, or re-exported wrappers, allowing a real graduation to evade the contract machinery. Codex recommends replacing this with an explicit export manifest or an exhaustive surface scan.

**Consensus status: divergence** – Codex rates this **Major**; the other three reviewers all explicitly praise the same mechanism as sound, robust, and honest. This is a direct disagreement on the severity and nature of the issue.

---

### 🟡 Minor

#### 3. Shallow immutability of `technical_detail` in `ExplanationCard`
| Reviewer(s) | Severity | Exact reference |
|-------------|----------|----------------|
| **DeepSeek** | Minor | `reviews/deepseek/governance-scaffold-r2.md` – Finding 1 – cites `src/xai/cards.py` lines 82–84: `MappingProxyType(dict(self.technical_detail))` only shallow-freezes the mapping; nested mutable values can still be mutated. |
| Gemini       | Not raised (states deep-freeze is used) | – |
| Codex        | Not raised (states deep-freeze is used) | – |
| GLM          | Not raised | – |

**Description:**  
DeepSeek reports that the `MappingProxyType` wrapping only provides shallow immutability. Gemini and Codex both state that a **recursive** `_deep_freeze` is used, which prevents this issue. There is a clear disagreement on the current state of the code. If the deep freeze exists, DeepSeek’s finding is moot; if not, it is a valid Minor issue. The reports do not provide the source text, so this contradiction must be resolved by examining the actual file.

**Consensus status: divergence on factual state** – DeepSeek says shallow; Gemini and Codex say deep. No reviewer contests the underlying recommendation (deep freeze is better).

---

#### 4. LINV-004 enforcement column correctly aspirational
| Reviewer(s) | Severity | Exact reference |
|-------------|----------|----------------|
| **GLM**     | Minor    | `reviews/glm/governance-scaffold-r2.md` – Finding 2 – cites `docs/INVARIANTS.md:13`: LINV-004’s Enforcement column says “no automated gate yet — aspiration until the preprocessing modules graduate with indicator tests.” GLM notes this is correctly documented as aspirational. |
| Others      | Not raised | – |

**Description:**  
GLM flags this as a Minor finding but recommends no change. No other reviewer mentions LINV-004 specifically. This finding is neutral; it does not indicate a defect.

---

#### 5. CI workflow duplicates `verify.py` logic
| Reviewer(s) | Severity | Exact reference |
|-------------|----------|----------------|
| **Gemini**  | Minor    | `reviews/gemini/governance-scaffold-r3.md` – Finding 1 – cites `.github/workflows/ci.yml` (entire file): the CI reimplements the check sequence that `verify.py` already orchestrates, creating two sources of truth. Recommends refactoring the CI job to call `python tools/verify.py` directly. |
| Others      | Not raised | – |

**Description:**  
Only Gemini raises this. The other reviewers did not comment on CI duplication.

---

#### 6. `RejectionCard.missing_requirements` value validation missing
| Reviewer(s) | Severity | Exact reference |
|-------------|----------|----------------|
| **Codex**   | Minor    | `reviews/codex/governance-scaffold-r3.md` – Finding 3 – cites `src/xai/cards.py#L146`: `__post_init__` coerces `missing_requirements` to a tuple but never validates items are non-empty strings; a bare string becomes a tuple of characters. Recommends validating item types and explicitly rejecting `str`/`bytes`. |
| Others      | Not raised | – |

**Description:**  
Solo finding by Codex. No other reviewer mentions this validation gap.

---

### ⚪ Nit

#### 7. Missing module docstring in `src/xai/base.py`
| Reviewer(s) | Severity | Exact reference |
|-------------|----------|----------------|
| **GLM**     | Nit      | `reviews/glm/governance-scaffold-r2.md` – Finding 1 – cites `src/xai/base.py:1-33`: lacks a module docstring explaining the ABC contract. Recommends adding one. |
| Others      | Not raised | – |

**Description:**  
Minor documentation gap. No disagreement.

---

## Per-Reviewer Verdict Table

| Reviewer | Verdict / Severity Summary |
|----------|----------------------------|
| **DeepSeek** | **APPROVE** – 1 Minor (shallow freeze). No Blockers or Majors. |
| **GLM**      | **APPROVE** – 1 Nit (missing docstring), 1 Minor (LINV-004 correctly aspirational). No Blockers or Majors. |
| **Gemini**   | **APPROVE-WITH-CHANGES** – 1 Minor (CI duplication). No Blockers or Majors. |
| **Codex**    | **REQUEST-CHANGES** – 2 Majors (invariant overstatement, brittle discovery), 1 Minor (missing validation). No Blocker. |

---

## Consensus Verdict

**approve-after-fixes**  
Two reviewers approve unconditionally (DeepSeek, GLM). Gemini approves after a minor CI change. Codex requests changes for two Major issues. Because at least one Major is raised and no reviewer rates the package Blocker, the package is **not acceptable as drafted** and requires fixes. After addressing the two Major findings, the package would be ready.

---

## Action Before Sign-Off (every Blocker or Major raised by at least one reviewer)

1. **Resolve invariant automation overstatement** (Codex Major – LINV-005, LINV-007)  
   - Either tighten the wording in `docs/INVARIANTS.md` to match the actual scope of `tools/check_data_cards.py` and `tools/check_guidelines.py`, or add dedicated enforcement for provenance fields and config coverage.

2. **Fix graduation/stub-discovery brittleness** (Codex Major – `test_stub_contracts.py`, `check_notebooks.py`)  
   - Replace or supplement the current dynamic discovery with an explicit export manifest or an exhaustive surface scan that cannot miss inherited methods, callable objects, or re-exported wrappers.

---

**Reminder:** A single reviewer’s clearance (e.g., APPROVE) is not sign-off; all findings must be resolved or overruled by a designated decision-maker.
