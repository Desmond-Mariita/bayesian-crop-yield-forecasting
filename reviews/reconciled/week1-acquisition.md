# week1-acquisition

> Generated by aichat + DeepSeek (`deepseek:deepseek-v4-flash`, role `auditor`) on 2026-07-08 00:42:47 CEST.
> Inputs (5): reviews/deepseek/week1-acquisition.md reviews/gemini/week1-acquisition.md reviews/codex/week1-acquisition.md reviews/glm/week1-acquisition.md reviews/kimi/week1-acquisition.md

---

# Reconciled Review Report: week1-acquisition

## Deduplicated Findings (ordered by highest severity any reviewer assigned)

---

### 1. (Major – Divergence) POWER failure path test coverage gap  
**Reviewer(s) and severity assigned:**  
- **Codex (Major):** Raises a real test-quality gap.  
- **DeepSeek** – states test quality **Clean** (check 2).  
- **Gemini** – states test quality **Clean** (check 2).  
- **GLM** – states test quality **CLEAN** (check 2).  

**Exact references:**  
- Codex: `tests/unit/test_weather_acquisition.py:135` (`test_fetch_failure_propagates`) – the test monkeypatches `_fetch_json` instead of `urllib.request.urlopen`, so the real transport error‑mapping path of `download_weather_data` is not exercised through the fake‑HTTP layer.  
- DeepSeek: review, *Check 2* – “**Clean.** All tests are genuinely offline … Both failure paths are exercised … No untested branch of significance …”  
- Gemini: review, *Check 2* – “**Clean.** … They effectively pin the required behavior … No significant untested branches were found.”  
- GLM: review, *Check 2* – “**✅ CLEAN** … error handling is fully exercised.”  

**Position:** Codex considers this a **Major** gap because a regression in the POWER entry point’s transport wiring could go undetected. The other three reviewers evaluated the same test file and concluded that the failure coverage is complete and the gap is not significant. The divergence is **not** on whether the test exists (all agree it does) but on whether the existing test is sufficient.

**Proposed resolution:** The reconciler notes the majority view is that the test quality is clean. However, because Codex flagged this as Major, it is included in the “Action before sign‑off” list below; the team should decide whether to add POWER‑specific fake‑HTTP coverage.

---

### 2. (Minor) Data‑card date‑range discrepancy  
**Reviewer(s):** DeepSeek (Minor).  
**Exact references:**  
- `docs/data_cards/DC-usda-nass-quickstats.md`, line `date_range: 2000-2025`.  
- `src/data/acquisition.py` default `year_start=1990`.  
**Issue:** The card states a planned pull window of 2000‑2025, but the code defaults to 1990.  
**Recommendation:** Align the card or the default so they are consistent.

---

### 3. (Minor) Hardcoded NASS error messages in `_fetch_json` misattribute POWER failures  
**Reviewer(s):** Gemini (Minor).  
**Exact references:**  
- `src/data/acquisition.py`, lines 90 and 94: error messages mention “NASS Quick Stats” even when `_fetch_json` is called on behalf of `download_weather_data`.  
**Issue:** A POWER outage would raise a misleading error.  
**Recommendation:** Generalise the messages to be source‑agnostic (e.g., “API request failed”).

---

### 4. (Minor) Stale module docstring claim about `data_source` tagging  
**Reviewer(s):** Codex (Minor).  
**Exact references:**  
- `src/data/acquisition.py:10-13` – module docstring says every record carries `data_source` `api_nass` and points only to the NASS card, but the module now also writes POWER rows tagged `api_nasa_power`.  
**Issue:** Misleading statement.  
**Recommendation:** Update to mention both tags explicitly.

---

### 5. (Minor) INVARIANTS.md overstates notebooks‑execute gate activation  
**Reviewer(s):** GLM (Minor).  
**Exact references:**  
- `docs/INVARIANTS.md:28` – “active since 2026-07-08”.  
- `tools/verify.py:113-128` – the gate is **conditional** on notebooks existing (otherwise `DEFERRED`).  
**Issue:** The wording implies unconditional enforcement.  
**Recommendation:** Rephrase to clarify that the gate runs only when notebooks are present.

---

### 6. (Nit) Silent column filtering in `_records_to_frame`  
**Reviewer(s):** DeepSeek (Nit).  
**Exact references:**  
- `src/data/acquisition.py:87` – `frame = frame.loc[:, [c for c in columns if c in frame.columns]]`.  
**Issue:** If an expected column is missing it is silently omitted.  
**Recommendation:** Add a guard that logs a warning or raises an error for missing required columns.

---

### 7. (Nit) Companion notebook does not display full source of `download_nass_yields`  
**Reviewer(s):** DeepSeek (Nit).  
**Exact references:**  
- `notebooks/data/acquisition.ipynb`, cell after “The shipped implementation”.  
**Issue:** Concatenation in `inspect.getsource` truncates the source of `download_nass_yields`.  
**Recommendation:** Use separate `Code` objects per public function.

---

### 8. (Nit) Outdated comment in test stub  
**Reviewer(s):** GLM (Nit).  
**Exact references:**  
- `tests/integration/test_acquisition_smoke.py:1` – module docstring says the file tests NASS only.  
**Issue:** Now also tests POWER (lines 62-79).  
**Recommendation:** Update comment to reflect dual coverage.

---

## Per‑Reviewer Verdict Table

| Reviewer / Tool | Verdict | Severity Summary |
|----------------|---------|------------------|
| **DeepSeek** (`deepseek:deepseek-v4-flash`) | APPROVE-WITH-CHANGES | 1 Minor, 2 Nits |
| **Gemini** (`gemini-2.5-pro`) | APPROVE-WITH-CHANGES | 1 Minor |
| **Codex** (Codex CLI, model `default`) | APPROVE-WITH-CHANGES | 1 Major, 1 Minor |
| **GLM** (`glm-4.7` via Z.ai) | APPROVE-WITH-CHANGES | 1 Minor, 1 Nit |
| **Kimi** (`kimi-k2.6` via Moonshot) | No findings submitted (plan only) | – |

---

## Consensus Verdict

**APPROVE-WITH-CHANGES** – All four reviewers who submitted findings agree on this verdict. The sole divergence is the severity of the POWER test coverage gap (Codex: Major; others: Clean), but this does not affect the overall consensus verdict.

---

## Action Before Sign‑Off

Every **Blocker** or **Major** raised by at least one reviewer must be addressed:

1. **Major – POWER failure path transport coverage** (Codex): Add a fake‑HTTP test for `download_weather_data` that exercises `urllib.request.urlopen` error paths (both `URLError` and invalid JSON) and asserts `ConnectionError` is raised. See `tests/unit/test_weather_acquisition.py:135` and the divergent positions above.

---

## Reminder

A single reviewer’s clearance is not sign-off.
