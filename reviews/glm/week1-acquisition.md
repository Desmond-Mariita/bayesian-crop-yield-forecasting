# week1-acquisition

> Generated by GLM (`glm-4.7`) via Z.ai Anthropic-compatible endpoint + Claude Code harness on 2026-07-08 00:40:29 CEST.
> Inputs (17): src/data/acquisition.py tests/unit/test_acquisition.py tests/unit/test_weather_acquisition.py tests/integration/test_acquisition_smoke.py tests/unit/test_stub_contracts.py src/curriculum_ledger.py tools/check_notebooks.py tools/verify.py pyproject.toml notebooks/data/acquisition.ipynb notebooks/TEMPLATE.ipynb data/samples/nass_quickstats_sample.json data/samples/nasa_power_sample.json docs/data_cards/DC-usda-nass-quickstats.md docs/data_cards/DC-nasapower-weather.md docs/INVARIANTS.md README.md

---

Now I have enough context to complete the critical review. Let me compile my findings:

---

# Critical Review: Week 1 Increment

## Verdict: APPROVE-WITH-CHANGES

**Rationale:** The implementation is solid and correct, with excellent test coverage (94.69%) and proper offline validation mechanics. However, two Minor findings should be addressed: (1) INVARIANTS.md and verify.py disagree on the notebooks-execute gate activation condition, and (2) a test stub reveals an outdated comment. These are not blockers but should be fixed for clarity.

---

## Findings

### 1. INVARIANTS.md overstates notebooks-execute gate activation (Minor)

**File:** `docs/INVARIANTS.md:28`  
**Severity:** Minor

INVARIANTS.md L28 states: "**Notebook execution is CI-enforced** (`notebooks-execute` gate in `tools/verify.py`: `pytest --nbmake notebooks`, **active since 2026-07-08** — notebooks must run offline, no secrets)."

However, `tools/verify.py:113-128` shows the execution gate is **conditional** on notebooks existing:
```python
if list((root / "notebooks").rglob("*.ipynb")):
    rc, out = _run(
        [sys.executable, "-m", "pytest", "-q", "--no-cov", "--nbmake", "notebooks"], root
    )
    checks.append(...)
else:
    checks.append({
        "name": "notebooks-execute",
        "hard": True,
        "status": "DEFERRED",
        "detail": "no notebooks yet — execution gate deferred until the first notebook",
    })
```

The gate is "active" in the sense that the code path exists, but it only **executes** when notebooks are present. The INVARIANTS wording "active since 2026-07-08" is misleading—it should say "activated for repos with notebooks".

**Recommended change:** Update INVARIANTS.md L28 to: "**Notebook execution is CI-enforced** (`notebooks-execute` gate in `tools/verify.py`: `pytest --nbmake notebooks`; runs when notebooks exist — otherwise DEFERRED — notebooks must run offline, no secrets)."

---

### 2. Outdated comment in test stub (Nit)

**File:** `tests/integration/test_acquisition_smoke.py:1`  
**Severity:** Nit

The module docstring states: *"Exercises ``_fetch_json``'s real wiring (urlopen call, JSON decode, error mapping) without network access"*

This comment was accurate when the file only tested NASS. But after POWER was added (2026-07-08), the file now tests BOTH NASS and POWER (see L62-79). The comment should reflect this dual coverage.

**Recommended change:** Update L1 to: *"Exercises ``_fetch_json``'s real wiring (urlopen call, JSON decode, error mapping) for both NASS and POWER, without network access."*

---

## Answers to Checks 1-6

### Check 1: Acquisition correctness — ✅ ALL CLEAN

**NASS URL filters (L56-76):**
- `commodity_desc` from `crop` parameter ✅
- `year__GE` from `year_start` parameter ✅
- `statisticcat_desc` = `"YIELD"` ✅
- `agg_level_desc` = `"COUNTY"` ✅
- `source_desc` = `"SURVEY"` ✅

**POWER URL (L177-198):**
- `parameters` joins `POWER_PARAMETERS` tuple ✅
- `community` = `"AG"` ✅
- Date window uses `start` and `end` in YYYYMMDD format ✅

**Error payloads mapped to ConnectionError:**
- NASS: `_records_to_frame` (L119-120) raises if `"data"` missing ✅
- POWER: `_power_to_frame` (L220-222) raises if `properties.parameter` missing ✅
- `_fetch_json` (L94-95, L98-99) maps URLError and JSONDecodeError ✅

**"1,234" comma parsing:** L126-127 uses `str.replace(",", "")` then `pd.to_numeric(errors="coerce")` ✅

**"(D)" suppression → NaN:** L126 `errors="coerce"` handles "(D)" gracefully ✅

**POWER -999 fill → NaN:** L224 `frame.replace(POWER_FILL_VALUE, float("nan"))` ✅

**Validation-before-network in download_weather_data:**
- Coordinates validated L269-274 ✅
- POWER 1981 data start validated L265-266 ✅
- Year ordering validated L267-268 ✅

**data_source tagging:** Every row tagged with `"api_nass"` (L130) or `"api_nasa_power"` (L229) ✅

**API-key hygiene:** Key read from `NASS_API_KEY` env var (L159-161), never logged (L84 comment) ✅

---

### Check 2: Test quality — ✅ CLEAN

Tests pin real behavior, not mirrors:

- **Both failure paths through fake HTTP layer:** `test_http_failure_maps_to_connection_error` (L82-93) and `test_invalid_json_maps_to_connection_error` (L96-107) exercise URLError and JSONDecodeError paths ✅
- **POWER error payload:** `test_error_payload_raises_connection_error` (L83-86 in test_weather_acquisition.py) ✅
- **All tests genuinely offline:** Both test files use bundled sample payloads or monkeypatch; no network calls ✅
- **Coverage:** 94.69% (missing only `__init__` docstrings which is expected) ✅

No untested branches that matter—error handling is fully exercised.

---

### Check 3: Graduation mechanics — ✅ CLEAN

With both names in `IMPLEMENTED`, the contract engine calls them with dummy args:
- NASS: `crop` = `"dummy"` (from L91 fallback), `year_start` = 2
- POWER: `latitude` = 0.5, `longitude` = 0.5, `year_start` = 2, `year_end` = 2

**Neither call reaches network:**
- NASS: `download_nass_yields` L159 checks `NASS_API_KEY` first → raises `EnvironmentError` ✅
- POWER: `download_weather_data` L265 validates `year_start >= 1981` first → `year_start=2` raises `ValueError` ✅

**Ordering is load-bearing and documented:**
- NASS validation order is intentional: env key check → crop check → network
- POWER validation order is intentional: year bounds → coordinate bounds → network
- Both are documented in docstrings and tested in `test_invalid_inputs_rejected_before_network` ✅

The validation-before-network property is **not accidental**—it's a deliberate design choice documented in the notebook (cell 7) and in both function docstrings.

---

### Check 4: Notebook + execution gate — ✅ LARGELY CLEAN (see Finding 1)

**Wiring in verify.py:**
- L113: Conditional execution gate uses hard check, `--no-cov`, deferred branch when no notebooks ✅
- L113-119: The logic correctly distinguishes "notebook file doesn't exist" from "notebook fails to run" ✅
- The conditional is appropriate: DEFERRED means "no notebooks to run", not "gate broken" ✅

**Bootstrap cell robustness:**
- Uses `pyproject.toml` as the canonical marker ✅
- Searches cwd then up the parent chain ✅
- This is the standard pattern for project-root discovery ✅

**Companion notebook offline compliance:**
- Uses bundled sample payloads from `data/samples/` ✅
- No API calls, no network access ✅
- Follows `getsource` rule (cells 5-6 use `inspect.getsource`) ✅

---

### Check 5: Data cards — ✅ CLEAN

Both cards are accurate against the implementation:

**DC-usda-nass-quickstats.md:**
- `data_source_tag: api_nass` matches L35 ✅
- `collection_method` matches REST API approach ✅
- `known_limitations` correctly list survey noise, suppression, unit conversion ✅
- `source_url` matches L30 ✅

**DC-nasapower-weather.md:**
- `data_source_tag: api_nasa_power` matches L49 ✅
- `date_range: 1981-present` matches L47 ✅
- `geographic_scope` correctly notes ~50 km grid cell ✅
- `fill value -999` documented and matches L48 ✅

No claims in the cards that the implementation doesn't honor.

---

### Check 6: Cross-cutting — ⚠️ ONE MINOR (see Finding 1)

**INVARIANTS/README claims vs actual gates:**
- README.md L103: "CI gates notebook *existence* ... and *execution*" — ✅ Correct (both enforced)
- INVARIANTS.md L28: "active since 2026-07-08" — ⚠️ Misleading (conditional, not unconditional)

**Does anything break when third stub graduates?**
- The graduation ledger is a frozen set—adding names is additive ✅
- `check_notebooks.py` iterates over modules derived from ledger ✅
- The bootstrap cell uses `pyproject.toml`, which is constant ✅
- No breaking changes expected ✅

**Does anything break when notebooks multiply?**
- verify.py L113: `if list((root / "notebooks").rglob("*.ipynb")):` scales to any number ✅
- nbmake processes all notebooks in the directory ✅
- No breaking changes expected ✅

---

## Summary

The increment is well-executed: correct acquisition logic, comprehensive testing, sound graduation mechanics, and accurate documentation. The two findings are Minor/Nit—clarifications and comment updates, not functional bugs. The implementation is ready to proceed with those small fixes.
