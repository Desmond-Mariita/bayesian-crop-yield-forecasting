# governance-scaffold

> Generated by GLM (`glm-4.7`) via Z.ai Anthropic-compatible endpoint + Claude Code harness on 2026-07-07 06:24:38 CEST.
> Inputs (22): README.md research_log.md docs/INVARIANTS.md docs/learning-resources.md docs/DEVELOPER_GUIDELINES.txt src/xai/__init__.py src/xai/cards.py src/xai/gates.py src/xai/base.py tests/unit/test_xai_cards.py tests/unit/test_xai_gates.py tests/unit/test_stub_contracts.py tests/unit/test_stub_private_branches.py tests/integration/test_xai_smoke.py tools/verify.py tools/check_data_cards.py tools/check_notebooks.py .github/workflows/ci.yml notebooks/TEMPLATE.ipynb config/crops/kilifi_crops.yaml docs/data_cards/DC-usda-nass-quickstats.md docs/data_cards/DC-kalro-kilifi-crops.md

---

My comprehensive review is complete. I've analyzed all 18 files against the 7 specified checks and documented my findings in the plan file.

## Review Summary

**Verdict:** APPROVE-WITH-CHANGES

**Rationale:** The XAI contract, gate tools, data cards, and config are fundamentally sound and well-tested. The governance structure faithfully mirrors the production platform. However, several issues should be addressed before the first stub graduates.

### Findings (7 total)

**Major (2):**
1. **Convergence gate type mismatch** - `evaluate_convergence` signature says `divergences: int` but implementation stores it as float; this is confusing and the `failed` list logic is inconsistent.
2. **Data-card validator creates empty lists from empty strings** - a card with `- ""` list items passes validation (non-empty list) but is semantically empty.

**Minor (4):**
3. Type hint inconsistency in `evaluate_convergence` doesn't match internal float conversion.
4. Coverage warning not documented for the stub NotImplementedError pattern - 90% gate passes before real implementation exists.
5. `DataQuality` enum value not constrained in `ExplainableModel` return type hint (nit).
6. "Infrastructure modules" in LINV-010 is undefined - unclear which modules are exempt from the notebook requirement.

**Minor (1):**
7. Test gap for empty-list field validation and `technical_detail` Mapping field in `test_xai_cards.py`.

### Key Answers by Check

1. **src/xai correctness:** Sound overall. Confidence bounds correct, frozen dataclasses good, EvidenceGate >= semantics correct, absent keys default to 0.0, convergence thresholds match documented strict comparisons. Type inconsistency on divergences.

2. **Test quality:** Tests pin behavior, not mirror implementation. Stub-contract engine is sound and intentional. NotImplementedError coverage gives 100% coverage before real implementation - this is documented but lacks automated enforcement during graduation.

3. **Gate tools:** Frontmatter parser handles declared scope well (won't support nested YAML but that's by design). Notebooks AST coupling is intentional (feature, not bug). verify.py and ci.yml are consistent.

4. **Governance coherence:** 8 of 10 invariants are fully enforceable. LINV-003 and LINV-006 rely partially on human review for content justification, which is acceptable.

5. **Curriculum:** Coherent with charter. Realistic week counts. Evidence gates achievable (≥500 county-years). Learning resources well-matched to milestones.

6. **Config/data:** `kilifi_crops.yaml` structurally valid with complete provenance. Both data cards valid against schema. No inconsistencies with citing code/docs.

7. **Cross-cutting:** Strong consistency across README, INVARIANTS, research_log, and tools. Graduation path is well-designed. Main gap: automated enforcement of "replace stub test with real tests" during graduation.

The review is saved to the plan file with full details for each finding.
