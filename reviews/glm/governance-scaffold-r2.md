# governance-scaffold-r2

> Generated by GLM (`glm-4.7`) via Z.ai Anthropic-compatible endpoint + Claude Code harness on 2026-07-07 06:40:11 CEST.
> Inputs (23): README.md research_log.md docs/INVARIANTS.md docs/learning-resources.md docs/DEVELOPER_GUIDELINES.txt src/xai/__init__.py src/xai/cards.py src/xai/gates.py src/xai/base.py tests/unit/test_xai_cards.py tests/unit/test_xai_gates.py tests/unit/test_stub_contracts.py tests/unit/test_stub_private_branches.py tests/integration/test_xai_smoke.py tools/verify.py tools/check_guidelines.py tools/check_data_cards.py tools/check_notebooks.py .github/workflows/ci.yml notebooks/TEMPLATE.ipynb config/crops/kilifi_crops.yaml docs/data_cards/DC-usda-nass-quickstats.md docs/data_cards/DC-kalro-kilifi-crops.md

---

I've completed the comprehensive review of the 22 files against the 7 specified checks. Here are my findings:

---

## Verdict: APPROVE

**Rationale:** The changes establish a coherent governance framework with enforceable contracts and gates. The XAI module correctly implements the card contract with deep immutability; convergence thresholds are strict as documented; tests pin behavior well; CI gates are correctly wired; the curriculum plan is realistic; and the data cards/config are complete. There are only minor documentation gaps, no Blockers or Major findings.

---

## Findings

### 1. [Nit] Missing module docstring in src/xai/base.py
**Severity:** Nit  
**File:** src/xai/base.py:1-33  
**Issue:** The module lacks a docstring explaining the ABC contract. cards.py and gates.py have comprehensive module docstrings, but base.py does not.  
**Recommended change:** Add a module docstring: "Abstract base class making the XAI contract mandatory for every model (LINV-001)."

### 2. [Minor] Invariant LINV-004 enforcement column is aspirational, not mechanistic
**Severity:** Minor  
**File:** docs/INVARIANTS.md:13  
**Issue:** LINV-004's Enforcement column says "no automated gate yet — aspiration until the preprocessing modules graduate with indicator tests." This correctly notes the status is aspirational.  
**Recommended change:** None needed - the invariant is correctly documented as aspirational. This is the only invariant not yet enforceable by CI.

---

## Check-by-Check Results

### Check 1: src/xai correctness - CLEAN
- **Card validation:** Empty-field checks (cards.py:37-48) and confidence bounds (cards.py:51-63) are correct.
- **Frozen dataclasses:** Both ExplanationCard and GateStatus use MappingProxyType for deep immutability (cards.py:106, gates.py:46-47). Test at cards.py:65-73 confirms caller mutation doesn't leak.
- **EvidenceGate >= semantics:** Line 72 uses `<` for comparison, so boundary values MEET the gate. Test at test_xai_gates.py:41-44 confirms this.
- **evaluate_convergence thresholds:** Lines 140-147 enforce strict boundaries (r_hat < 1.01, ess > 400, divergences == 0). Boundary values FAIL. Matches documented criteria (research_log.md:35, gates.py:137-138, INVARIANTS.md:13). Test at test_xai_gates.py:97-105 confirms.

### Check 2: Test quality - CLEAN
- **Tests pin behavior:** test_xai_cards.py, test_xai_gates.py, and test_xai_smoke.py enforce invariants and contract behavior.
- **Stub-contract engine soundness:** The AST-free dynamic discovery (test_stub_contracts.py:114-130) correctly finds public callables. The test CATCHES when a stub is implemented (expects NotImplementedError), forcing you to add it to IMPLEMENTED and write real tests.
- **Coverage strategy:** Exercising stub raises ensures coverage is meaningful pre-implementation. Once you graduate a stub, you must write real tests. This is honest and correct.

### Check 3: Gate tools - CLEAN
- **check_data_cards.py parser:** Handles comments (line 62), colons in values (partition on first colon only, line 76), and block lists (lines 64-72). Minimal by design (no nested YAML), which is acceptable.
- **check_notebooks.py AST coupling:** Reading IMPLEMENTED from the test file is intentional and sound. The coupling is minimal and well-documented; it enforces the graduation workflow.
- **verify.py and ci.yml:** All gates are correctly wired and consistent. Coverage threshold of 90% matches across both files.

### Check 4: Governance coherence - CLEAN
- **10 invariants enforceable?**
  - LINV-001, 002, 003 (convergence), 005, 007, 008, 009, 010: Enforceable via code, tests, or CI.
  - LINV-003 (evidence gates) and LINV-006: Partially enforceable (checklists for evidence gates, model-card review for priors).
  - LINV-004: Aspirational (explicitly noted as "no automated gate yet").
- **No invariants are aspiration dressed as enforcement** - the two aspirational ones are correctly labeled.

### Check 5: Curriculum - CLEAN
- **Coherent with charter:** Phase 2 loop (derive by hand → PyMC → comparison notebook → model card) matches README.md:16-19 exactly.
- **Realistic week counts:** 13 weeks per phase is tight but appropriate for a full-time learning sprint.
- **Evidence gates:** Every milestone has evidence and convergence gates, pinned to src/xai/gates.py.
- **Learning resources match milestones:** Phase 1-4 resources align with their respective milestones.

### Check 6: Config/data - CLEAN
- **kilifi_crops.yaml:** Valid structure, complete provenance (global and per-crop basis/kalro_reference).
- **Data cards pass schema:** Both DC-usda-nass-quickstats.md and DC-kalro-kilifi-crops.md have all required scalar and list fields.
- **No inconsistencies:** README, INVARIANTS, research_log, and the tools are all aligned.

### Check 7: Cross-cutting - CLEAN
- **No contradictions:** Charter, convergence thresholds, CI gates, graduation path all align across files.
- **Graduation path is sound:** Implement stub → test_stub_contracts.py fails → add to IMPLEMENTED → write real tests + companion notebook → verify.py passes. The TDD nudge works.

---

## Summary

This is a well-governed learning repository that establishes production-grade contracts and gates. The only issues are a missing module docstring (Nit) and two correctly-labeled aspirational invariants (Minor). There are no Blockers or Major findings. The changes are ready to merge.
