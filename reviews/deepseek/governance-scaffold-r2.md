# governance-scaffold-r2

> Generated by aichat + DeepSeek (`deepseek:deepseek-v4-flash`, role `auditor`) on 2026-07-07 06:35:00 CEST.
> Inputs (23): README.md research_log.md docs/INVARIANTS.md docs/learning-resources.md docs/DEVELOPER_GUIDELINES.txt src/xai/__init__.py src/xai/cards.py src/xai/gates.py src/xai/base.py tests/unit/test_xai_cards.py tests/unit/test_xai_gates.py tests/unit/test_stub_contracts.py tests/unit/test_stub_private_branches.py tests/integration/test_xai_smoke.py tools/verify.py tools/check_guidelines.py tools/check_data_cards.py tools/check_notebooks.py .github/workflows/ci.yml notebooks/TEMPLATE.ipynb config/crops/kilifi_crops.yaml docs/data_cards/DC-usda-nass-quickstats.md docs/data_cards/DC-kalro-kilifi-crops.md

---

**Verdict: APPROVE**  
The changes are correct, well-tested, and internally coherent. The `src/xai` module correctly implements the card contract with thorough validation and strict convergence semantics. The gate tools are robust within their design scope, the invariants are honestly categorized regarding enforceability, the curriculum plan is realistic and matches the attached resources, and no cross-cutting contradictions exist. The single minor issue noted below does not block approval.

---

**Findings**

1. **Minor** — `src/xai/cards.py` lines 82–84: `MappingProxyType(dict(self.technical_detail))` only shallow-freezes the mapping; nested mutable values (e.g., a list inside a dict value) can still be mutated after construction. Consider recursive deep-freezing or documenting that the values of the mapping must be immutable. This does not affect current usage because the payloads used are flat and immutable, and the existing tests only verify shallow immutability.  
   *Recommended change:* Replace with a recursive freeze helper (e.g., `_deep_freeze` that wraps lists in tuples and dicts in `MappingProxyType`), or add a note in the docstring that `technical_detail` values should be immutable.

---

**Explicit Answers to Checks 1–7**

**Check 1 – `src/xai` correctness**  
- Card validation semantics (`cards.py`): `__post_init__` correctly rejects empty required strings (non-empty check via `_require_non_empty` using `strip()`) and out-of-range confidence. The `MappingProxyType` freezing is correct for the flat mappings used.  
- `EvidenceGate.evaluate` uses `>=` semantics (met if `observed >= required`), and absent keys default to `0.0` via `current.get(key, 0.0)` – all documented and tested.  
- `evaluate_convergence` enforces strict boundaries: `r_hat >= R_HAT_MAX` fails (so `r_hat=1.01` fails), `ess_bulk <= ESS_BULK_MIN` fails, `ess_tail <= ESS_TAIL_MIN` fails, `divergences > DIVERGENCES_MAX` fails — exactly matching the documented “R-hat < 1.01, ESS > 400, 0 divergences” (see test `test_boundary_values_fail_strictly`).  
- ABC contract (`base.py`): `ExplainableModel` declares `explain_or_reject` as abstract, returning `Union[ExplanationCard, RejectionCard]`. Semantics are correct.

**Check 2 – Test quality**  
- Unit tests for `cards.py` and `gates.py` pin behaviour precisely: they test valid construction, immutability, boundary values, absent keys, and each diagnostic failure mode. They do not simply mirror the implementation; they independently assert expected exceptions and status fields.  
- The stub-contract engine (`test_stub_contracts.py`) is sound: dynamic discovery uses `__module__` guards to avoid picking up inherited/imported objects; `_dummy_value` is robust for the expected annotation types; the discovery count guard (`>=40`) and the “no unknown implemented names” test prevent silent mis-discovery or typos. The `NotImplementedError` assertion honestly serves the 90% coverage gate because stubs’ `raise` lines are covered, and the skipped graduated names force new tests on implementation.  
- Integration smoke test (`test_xai_smoke.py`) exercises the full contract end‑to‑end and verifies determinism (LINV-008).

**Check 3 – Gate tools**  
- `check_data_cards.py`: The frontmatter parser correctly handles key-value scalars (including colons in values), block lists with `- `, comment lines (`#`), and empty lines. It does not handle nested/indented YAML, but the required schema does not use those – this is a deliberate minimal design and sufficient for the current cards.  
- `check_notebooks.py`: Reading the `IMPLEMENTED` ledger via AST is tightly coupled to the test file’s constant name and structure. This is acceptable because both are under the project’s control; the coupling is intentional to keep the graduation ledger as the single source of truth. The design is robust: if the constant is renamed or removed, the tool breaks immediately and visibly.  
- `verify.py` correctly wires subprocess calls, handles missing tools as `UNAVAILABLE`, and applies the coverage gate only when tests exist.  
- `ci.yml` consistently runs the same gates as `verify.py`, uses the same coverage-deferral logic (exit code 5 detection), and makes mypy advisory with `continue-on-error`.

**Check 4 – Governance coherence**  
- The 10 invariants in `INVARIANTS.md` are enforceable as stated in their *Enforcement* column. Where automation is absent (LINV-004, LINV-006, LINV-008), the column honestly notes “no automated gate yet”. The invariant table does not claim full automation for those rows. LINV-001, LINV-005, LINV-010 have concrete enforcement via `src/xai/`, `tools/check_data_cards.py`, and `tools/check_notebooks.py` respectively. No aspirational invariant is dressed as enforced.

**Check 5 – Curriculum**  
- The milestone plan in `research_log.md` is coherent with the charter: each Bayesian milestone follows the NumPy → PyMC → compare loop with evidence and convergence gates. Week counts (13+13+13+8 = 47 weeks) are realistic within the Feb–Dec period.  
- `learning-resources.md` matches the milestones: Phase 1 resources cover descriptive stats, anomaly detection, and agro‑met; Phase 2 resources include Statistical Rethinking, PyMC, and Kalman filter references for each sub‑milestone; Phase 3 and 4 resources cover DL from scratch and XAI respectively.  
- The “graduation rule” (real tests + companion notebook in same week) is consistently described in both the log and the invariants.

**Check 6 – Config/data**  
- `config/crops/kilifi_crops.yaml` is structurally valid YAML with `schema_version`, `provenance` (including `source_repo`, `adapted` date, `primary_reference`, `units`), `seasons`, and `crops`. Every crop entry carries `basis` and `kalro_reference` (LINV-007).  
- Both data cards (`DC-usda-nass-quickstats.md` and `DC-kalro-kilifi-crops.md`) include all required fields: `dataset_id`, `dataset_name`, `version`, `status` (“draft”, allowed), `data_source_tag`, `source_url`, `collection_method`, `date_range`, `geographic_scope`, `temporal_resolution`, `phase`, plus non‑empty lists for `known_limitations` and `intended_use`. No inconsistency is found between the cards and the code/docs that cite them.

**Check 7 – Cross-cutting coherence**  
- README, INVARIANTS, research_log, and the tools all align: the charter (“derive by hand, then validate in PyMC”) is mirrored in the milestone plan; the governance contracts (`ExplanationCard`/`RejectionCard`, evidence gates, data cards, companion notebooks) are consistently referenced and enforced; the coverage gate is deferred until tests exist.  
- No contradictions are present. The “graduation” path (implement a stub → move to `IMPLEMENTED` → write real tests → add companion notebook) will work without breakage because the stub-contract test skips implemented names, and `check_notebooks.py` expects the notebook at that point. The coverage gate will then require the new tests to maintain 90%.
