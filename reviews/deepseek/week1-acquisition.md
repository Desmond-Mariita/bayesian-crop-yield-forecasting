# week1-acquisition

> Generated by aichat + DeepSeek (`deepseek:deepseek-v4-flash`, role `auditor`) on 2026-07-08 00:36:57 CEST.
> Inputs (17): src/data/acquisition.py tests/unit/test_acquisition.py tests/unit/test_weather_acquisition.py tests/integration/test_acquisition_smoke.py tests/unit/test_stub_contracts.py src/curriculum_ledger.py tools/check_notebooks.py tools/verify.py pyproject.toml notebooks/data/acquisition.ipynb notebooks/TEMPLATE.ipynb data/samples/nass_quickstats_sample.json data/samples/nasa_power_sample.json docs/data_cards/DC-usda-nass-quickstats.md docs/data_cards/DC-nasapower-weather.md docs/INVARIANTS.md README.md

---

## Verdict: APPROVE-WITH-CHANGES

The increment is well‑structured: both acquisition functions are correct, the tests cover happy paths and both failure modes, the graduation‑mechanics ordering is load‑bearing and tested, the notebook execution gate is properly wired, and data cards are accurate. A minor data‑card date‑range inconsistency and two nits (column filtering robustness, companion notebook display coverage) warrant small fixes before the next merge.

---

## Findings

### 1. (Minor) Data‑card date‑range discrepancy  
**File:** `docs/data_cards/DC-usda-nass-quickstats.md`, line `date_range: 2000-2025`  
**Issue:** The card states a planned pull window of 2000‑2025, but `download_nass_yields` defaults to `year_start=1990` and the sample includes 2021‑2023.  
**Recommendation:** Either update the card to say `1990‑present (default pull starts 1990)` or change the code default to 2000 so the card is a faithful description.

### 2. (Nit) Silent column filtering in `_records_to_frame`  
**File:** `src/data/acquisition.py`, line 87: `frame = frame.loc[:, [c for c in columns if c in frame.columns]]`  
**Issue:** If an expected column (e.g. `county_ansi`) is absent from the API response, it is silently omitted. This could cause a confusing `KeyError` later (e.g. in the `.rename()` call, though `.rename` is safe).  
**Recommendation:** Add a guard that logs a warning or raises an explicit error when a required column is missing, or use `reindex` with `fill_value` defaults.

### 3. (Nit) Companion notebook does not display `download_nass_yields`’ full source  
**File:** `notebooks/data/acquisition.ipynb`, cell after “The shipped implementation”  
**Issue:** The `Code(inspect.getsource(...))` block shows `_build_query_url`, `_records_to_frame`, *and* `download_nass_yields` – that is correct. However, the second block (`_build_power_url`, `_power_to_frame`, `download_weather_data`) also shows `download_weather_data`. Because `inspect.getsource` concatenates text, the source of `download_nass_yields` is truncated by the first block’s concatenation and not shown in full. (The module docstring notes both functions, so no functional information is lost, but the notebook does not display the full documentation of the public function.)  
**Recommendation:** Consider adding a separate cell for `download_nass_yields` alone, or use separate `Code` objects per public function to make the inline source complete.

---

## Answers to Checks

### 1. Acquisition correctness (NASS, POWER, error mapping, comma parsing, suppression, fill values, validation ordering, data source tagging, API‑key hygiene)

**Clean.** All checks pass:  
- **NASS URL** uses `source_desc=SURVEY`, `year__GE`, `statisticcat_desc=YIELD`, `agg_level_desc=COUNTY`, `format=JSON`.  
- **POWER URL** embeds the parameter list, `community=AG`, `start`/`end` in YYYYMMDD, `format=JSON`.  
- **Error payloads** (missing `data` in NASS, missing `properties.parameter` in POWER) raise `ConnectionError`.  
- **Comma parsing** (`"1,234"`) is stripped via `.str.replace(",","", regex=False)` and converted with `pd.to_numeric(errors="coerce")`.  
- **Suppression** `"(D)"` becomes NaN via the same `pd.to_numeric` coercion.  
- **POWER -999** is replaced with `NaN` via `.replace(POWER_FILL_VALUE, float("nan"))` before any row leaves `_power_to_frame`.  
- **Validation‑before‑network** in `download_weather_data` checks coordinates, `year_start >= 1981`, `year_end >= year_start` before a single URL is built.  
- **Data‑source tagging** is enforced: every record gets either `"api_nass"` or `"api_nasa_power"` via an explicit column assignment.  
- **API key** is read from environment variable `NASS_API_KEY`; the constructed URL contains the key but never appears in log messages (the log string only says “requesting NASS …”).  

### 2. Test quality (branch coverage, offline, failure paths)

**Clean.** All tests are genuinely offline (no network): they either monkey‑patch internal helpers (`_fetch_json`, `urllib.request.urlopen`) or use bundled sample files.  
- Both **failure paths** are exercised:  
  - `download_nass_yields`: missing API key, unknown crop, fetch failure (both URLError and invalid JSON), error payload.  
  - `download_weather_data`: invalid coordinates, year before 1981, year_end < year_start, fetch failure, error payload.  
- Every test that involves the public function either inherits a mocked HTTP layer or validates pre‑network guards.  
- The **integration smoke tests** (`test_acquisition_smoke.py`) exercise the full `urlopen`‑to‑save path via a fake HTTP response, verifying that the wiring (URL building, JSON decoding, error mapping) works end‑to‑end.  
- No untested branch of significance: the comma‑parsing, suppression, and fill‑value branches are each pinned by a dedicated test.  

### 3. Graduation mechanics (contract engine calls with dummy args, no network reachable, ordering documented/tested)

**Clean.** With both names in `IMPLEMENTED`:  
- `download_nass_yields` is invoked with *no arguments* (defaults: `crop="CORN"`, `year_start=1990`). Because `NASS_API_KEY` is not set, the environment‑variable check raises `EnvironmentError` before any network or validation. This guard’s ordering *is the safety net*.  
- `download_weather_data` is invoked with dummy floats/int (latitude=0.5, longitude=0.5, year_start=2, year_end=2). The `year_start < 1981` validation raises `ValueError` before any URL is constructed. This validation ordering is explicitly tested by `test_invalid_inputs_rejected_before_network` (parametrized with bad latitude, longitude, year_start, year_end).  
- Neither call ever reaches a network socket. The ordering is **load‑bearing** and is documented both in the function docstring (“validated before any network use”) and in the unit tests. The contract suite tolerates these exceptions (they are not `NotImplementedError`), so graduation stays green.  

### 4. Notebook + execution gate (verify.py wiring, bootstrap cell, getsource rule, offline rule)

**Clean.**  
- **verify.py** has a `notebooks-execute` hard gate that runs `pytest --no-cov --nbmake notebooks` when notebooks exist. The branch for empty notebooks is correctly deferred.  
- **Bootstrap cell** in `notebooks/data/acquisition.ipynb` uses `Path.cwd()` scanning ancestors for `pyproject.toml`; this works when the notebook is run from `notebooks/data/` (as nbmake does) because `pyproject.toml` is in the repository root.  
- **getsource rule** is honoured: source is displayed via `inspect.getsource` (never pasted).  
- **Offline rule** is honoured: the notebook only reads bundled sample files; no API calls, no secrets. The included assertions pass against the sample data.  

### 5. Data cards (accuracy against code, parameters, units, limitations, tags)

**Generally clean** – one minor inconsistency (Finding 1 above).  
- **DC-usda-nass-quickstats.md**: correctly documents `data_source_tag: api_nass`, source URL, collection via REST API, survey edition (not census), suppression kept as NaN, unit `bu/acre`. The card states a planned pull window `2000-2025` while the code defaults to `year_start=1990`; this is aspirational/planning language, not an implementation error.  
- **DC-nasapower-weather.md**: correctly documents `data_source_tag: api_nasa_power`, POWER API URL, AG community units (degC, %, MJ m⁻² day⁻¹, m s⁻¹), daily resolution, 1981 start, fill‑value -999 → NaN, no precipitation. Matches the code.  
- Both cards list known limitations that the implementation does not contradict.  

### 6. Cross‑cutting (INVARIANTS/README claims vs actual gates; third‑stub or multiple‑notebook future)

**Clean.**  
- **INVARIANTS.md** LINV‑010 states: “Notebook execution is CI‑enforced (`notebooks-execute` gate in `tools/verify.py`: `pytest --nbmake notebooks`, active since 2026-07-08)”. This matches the current `verify.py`. The notebook‑existence gate (`tools/check_notebooks.py`) is also wired and reports PASS for the two graduated names (each has a notebook and a dedicated test).  
- **README.md** quality section claims that CI gates notebook existence, dedicated tests, and execution – all satisfied.  
- The graduation ledger (`IMPLEMENTED`) contains only the two Week‑1 names, so the contract engine and notebook checker will work correctly when a third stub graduates: the new name must be added to `IMPLEMENTED`, have a notebook at the mirrored path, and be referenced in a test file outside the contract suites. The `verify.py`’s notebook‑execution gate will automatically pick up any new notebook because it globs `notebooks/**/*.ipynb`. No breakage is expected.
